[project]
name = "monorepo-mixed-python"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "django>=5.2,<6.0",
    "requests>=2.32,<3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"